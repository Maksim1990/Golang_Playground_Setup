# Golang playground setup
##### Golang dockerized setup


**HOT TO INSTALL APP**
--

* *Start app and build required Docker containers:*
``make install``

* *Exec into Golang container:*
``make exec``

* *Start app and build required Docker containers:*
``go run main.go``